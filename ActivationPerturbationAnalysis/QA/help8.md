已知context/ActivationPerturbationAnalysis/prompt/feishu1.json，context/ActivationPerturbationAnalysis/prompt/feishu2.json是多轮对话下的两个请求的结果。请你提取他们的prompt并对比，查找区别（确认哪些section不同），分析相似框架，请你理解我的目标并自行优化方案。


● 分析完成。核心发现：                
                                                                                          
  三次 API 调用（feishu1 = 1次, feishu2 = 2次）来自同一对话，System Prompt 完全相同。
                                                                                                                      
  关键结论：
                                                                                                                      
  ┌───────────────────────────────────────────┬──────────────────────────────────────────┐                          
  │                 对比维度                  │                   结果                   │
  ├───────────────────────────────────────────┼──────────────────────────────────────────┤
  │ System Prompt (30,898 chars, 70 sections) │ 三次调用 逐字符一致                      │
  ├───────────────────────────────────────────┼──────────────────────────────────────────┤
  │ 对话历史                                  │ 严格前缀追加：18 → 20 → 22 条消息        │
  ├───────────────────────────────────────────┼──────────────────────────────────────────┤
  │ Prefix cache 效率                         │ 91.9% ~ 99.8%                            │
  ├───────────────────────────────────────────┼──────────────────────────────────────────┤
  │ 跨对话 cache 失效 (help7)                 │ 本数据无法验证 — 需要不同 chat_id 的日志 │
  └───────────────────────────────────────────┴──────────────────────────────────────────┘

  关键细节：
  - feishu2.json 包含 2个 JSON 对象（同一用户请求触发的两次连续调用：先回复，再 tool_call + 继续）
  - scheduler.py 代码 (9,022 chars) 作为 tool 结果从第一次调用起就每次重复携带，占 prompt ~14%
  - 这三次调用构成完美的 triplet chain，可直接用于隐藏状态实验

  要验证 help7 的 Inbound Metadata 导致 Project Context cache 失效的问题，需要来自不同飞书群的两次调用日志。

  完整报告已写入 prompt/feishu_comparison_report.md。




  请你对比/root/context/ActivationPerturbationAnalysis/prompt/trace.json中，两个sub-agent的prompt的区别，（1）分析二者的prompt框架，按顺序分为哪些sections；（2）二者之间如果要按照之前的方式进行真实情况下的prompt的中间激活相似度比较，比如说第二个agent的第2次prompt相较于第一个agent的第2次prompt变化的部分在哪里，变化后的中间激活和第一个的中间激活对比，余弦相似度怎么样？这样的对比方式正确吗？请你理解我的目标（利用中间激活的相似度，在split inference场景下，即本地1-云端-本地2，；例如在本地1-云端通过传输中间激活的差值并且在云端恢复来减少传输通信量），那么这个设计的中间激活对比方式是否能合理的测试不同sub agent可以利用中间激活？请你优化完善我的plan